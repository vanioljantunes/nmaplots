% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gemtc.R
\name{nma_gemtc}
\alias{nma_gemtc}
\title{Build a gemtc network from treatment names as they are}
\usage{
nma_gemtc(data.ab = NULL, data.re = NULL, ...)
}
\arguments{
\item{data.ab}{Arm-level data, one row per study arm: \code{study}, \code{treatment}
and the outcome columns gemtc expects (\code{responders} and \code{sampleSize} for
binary outcomes; \code{mean}, \code{std.dev} and \code{sampleSize} for continuous ones).}

\item{data.re}{Optional relative-effect data: \code{study}, \code{treatment}, \code{diff}
and \code{std.err}.}

\item{...}{Further arguments for \code{\link[gemtc:mtc.network]{gemtc::mtc.network()}}, such as
\code{description} or \code{studies}.}
}
\value{
An \code{mtc.network} object. Its \code{treatments} table has \code{id} (the valid
id) and \code{description} (the name as given).
}
\description{
\code{\link[gemtc:mtc.network]{gemtc::mtc.network()}} only accepts treatment ids made of letters, digits
and underscores, so names such as \code{"Cilofexor + Firsocostat"} or
\code{"5-FU"} are rejected. \code{nma_gemtc()} takes the data with the names you
already use, makes valid ids internally and stores the original names in
the treatments' \code{description} column. \code{\link[=nmaplot]{nmaplot()}} shows those names on the
plot, and its \code{reference}, \code{order} and \code{highlight} arguments accept them
too, so the ids never need to be seen.
}
\examples{
if (requireNamespace("gemtc", quietly = TRUE)) {
  data(mash)
  d <- mash$fib_improvement_alldoses
  network <- nma_gemtc(d[, c("study", "treatment", "responders", "sampleSize")])
  network$treatments
  nmaplot(network, reference = "Placebo")
}
}
