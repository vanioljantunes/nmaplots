% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nmaplots-package.R
\docType{package}
\name{nmaplots-package}
\alias{nmaplots}
\alias{nmaplots-package}
\title{nmaplots: Publication-quality network plots for netmeta and gemtc objects}
\description{
Draws the evidence network of a network meta-analysis fitted with
\pkg{netmeta} or \pkg{gemtc}. One function, \code{\link[=nmaplot]{nmaplot()}}, takes the same
object as \code{\link[netmeta:netgraph]{netmeta::netgraph()}} (or a gemtc network, model or result) and
renders it with \pkg{ggplot2}: node area by
sample size, edge width and a circled count by number of studies, the
treatment name and \code{event/n = ...} at every node, share of patients inside the disc, an optional outer ring per
node showing a subgroup composition (study design, risk of bias, region,
anything you can count per treatment), a boxed legend under the network,
and export to PNG, PDF or TIFF. Nothing statistical is recomputed; every
number comes from the fitted object.
}
\section{Guides (read in order)}{

\enumerate{
\item \link{nmaplots-1-workflow} -- from a \code{netmeta} or \code{gemtc} object to a saved figure.
\item \link{nmaplots-2-ring} -- adding an outer ring with a subgroup composition.
\item \link{nmaplots-3-style} -- layouts, colours, labels, legend and titles.
\item \link{nmaplots-4-netgraph} -- moving from \code{netgraph()}: argument map.
}
}

\section{Main function}{

\describe{
\item{\code{\link[=nmaplot]{nmaplot()}}}{Draw (and optionally save) the network plot. Returns a
\code{ggplot} object with the node, edge and ring tables in \verb{$nmaplot}.}
}
}

\section{Example data}{

\link{mash}: arm-level results of trials of drugs for MASH with fibrosis, four
outcomes, with a \code{design} column for the ring examples.
}

\examples{
if (requireNamespace("netmeta", quietly = TRUE)) {
  library(netmeta)
  data(mash)
  d <- mash$fib_improvement_alldoses
  p <- pairwise(treat = treatment, event = responders, n = sampleSize,
                studlab = study, data = d, sm = "RR")
  net <- netmeta(p, reference.group = "Placebo")
  nmaplot(net, outcome = "Fibrosis improvement without worsening of MASH")
}

}
\seealso{
Useful links:
\itemize{
  \item \url{https://github.com/vanioljantunes/nmaplots}
  \item Report bugs at \url{https://github.com/vanioljantunes/nmaplots/issues}
}

}
\author{
\strong{Maintainer}: Vanio Antunes \email{vanio.antunes.md@gmail.com}

Authors:
\itemize{
  \item Vanio Antunes \email{vanio.antunes.md@gmail.com}
}

}
\keyword{internal}
