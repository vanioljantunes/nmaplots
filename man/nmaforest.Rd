% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forest.R
\name{nmaforest}
\alias{nmaforest}
\title{Forest plot of the relative effects of a gemtc network meta-analysis}
\usage{
nmaforest(
  x,
  comparisons = c("panels", "reference", "pairs"),
  reference = NULL,
  order = NULL,
  level = 0.95,
  sm = NULL,
  exponentiate = NULL,
  style = c("jama", "revman"),
  labels = NULL,
  clean_labels = TRUE,
  label_wrap = NULL,
  columns = c("studies", "n"),
  column_labels = NULL,
  digits = 2,
  sort = c("none", "effect", "label"),
  xlim = NULL,
  xticks = NULL,
  clip = 10,
  ref_line = NULL,
  favours = NULL,
  outcome = NULL,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  title_size = 18,
  title_color = "#1F2A44",
  title_align = c("left", "center"),
  font_family = NULL,
  text_size = 10,
  label_color = "#1F2A44",
  point_color = "#1F2A44",
  point_fill = NULL,
  point_size = 2.4,
  line_width = 0.5,
  band_fill = "#F4F6F8",
  rule_color = "#8A939B",
  row_height = 0.26,
  label_width = NULL,
  column_width = NULL,
  plot_width = 0.3,
  effect_width = 0.19,
  background = "white",
  file = NULL,
  width = 10,
  height = NULL,
  dpi = 300
)
}
\arguments{
\item{x}{The result of \code{\link[gemtc:mtc.run]{gemtc::mtc.run()}}, or a table from
\code{\link[gemtc:relative.effect.table]{gemtc::relative.effect.table()}}.}

\item{comparisons}{Which comparisons to draw: \code{"panels"}, \code{"reference"} or
\code{"pairs"}. See Details.}

\item{reference}{Treatment the rows are compared with when
\code{comparisons = "reference"}, and the first block otherwise. Defaults to
the first treatment of the network. The name as you use it is accepted.}

\item{order}{Character vector putting the treatments in the order you want;
the ones you leave out keep their network order.}

\item{level}{Credible level of the interval. Ignored, with a warning, when
\code{x} is a relative effect table, which only holds the 95\% interval.}

\item{sm}{Name of the effect measure for the column head. Defaults to what
the likelihood and link imply ("Risk Ratio", "Odds Ratio", ...).}

\item{exponentiate}{Logical. Back-transform the estimates. Defaults to
\code{TRUE} for logarithmic links (logit, log, cloglog) and \code{FALSE} otherwise.}

\item{style}{\code{"jama"} or \code{"revman"}.}

\item{labels}{Optional character vector of display names for the
treatments, named by treatment or in the order of the network.}

\item{clean_labels}{Logical. Turn underscores in the names into spaces.}

\item{label_wrap}{Wrap the names at this many characters, \code{NULL} for no
wrapping. Wrapped names need a larger \code{row_height}.}

\item{columns}{Which count columns to show: any of \code{"studies"}, \code{"n"} and
\code{"events"}, or \code{"none"}. \code{"events"} replaces \code{"n"} with events over
total, and is dropped when the network has no event counts. The counts
are the direct evidence for that pair alone; a pair with only indirect
evidence shows a dash.}

\item{column_labels}{Optional column heads, one per column shown, replacing
the defaults.}

\item{digits}{Decimal places of the effect and its interval.}

\item{sort}{Order of the rows inside a block: \code{"none"}, \code{"effect"} or
\code{"label"}.}

\item{xlim}{Limits of the effect axis, on the reporting scale (for example
\code{c(0.5, 4)}). Defaults to the range of the intervals, cut at \code{clip}.}

\item{xticks}{Tick positions on the reporting scale.}

\item{clip}{Widest interval to draw, as a multiple of (or distance from)
the null value; intervals running past it get an arrowhead. \code{Inf} draws
everything.}

\item{ref_line}{Position of the vertical null line. Defaults to 1 on a
ratio scale and 0 otherwise. \code{NA} leaves it out.}

\item{favours}{Two strings printed under the axis, left and right, such as
\code{c("Favours treatment", "Favours comparator")}.}

\item{outcome}{One line under the title naming the outcome.}

\item{title, subtitle, caption}{Title block. \code{subtitle} defaults to
\code{outcome}.}

\item{title_size, title_color, title_align}{Title block size (points),
colour and alignment (\code{"left"} or \code{"center"}).}

\item{font_family}{Font family. Defaults to \code{"serif"} for the JAMA style
and \code{"sans"} for RevMan.}

\item{text_size}{Font size (points) of the rows.}

\item{label_color}{Colour of the row text.}

\item{point_color, point_fill, point_size}{Marker colour, fill and size. The
RevMan style sizes the marker by the direct sample size instead.}

\item{line_width}{Width of the interval lines.}

\item{band_fill}{Fill of the alternating row bands, \code{NA} for none.}

\item{rule_color}{Colour of the axis and the rules.}

\item{row_height}{Height of one row, in inches, used when \code{height} is not
given.}

\item{label_width, column_width, plot_width, effect_width}{Widths of the name
column, of one count column, of the plotting band and of the effect
column, as fractions of the figure width. \code{label_width = NULL} gives the
names whatever the other three leave over, so the band keeps its share
however many columns are shown; \code{column_width = NULL} picks a width that
suits the columns.}

\item{background}{Background colour.}

\item{file}{Path(s) to write: \code{.png}, \code{.pdf}, \code{.tiff}. \code{NULL} only returns
the plot.}

\item{width, height, dpi}{Size in inches and resolution of the saved figure.
\code{height} defaults to the number of rows times \code{row_height}.}
}
\value{
A \code{ggplot} object. The tables used for drawing are in
\code{p$nmaforest$rows} and \code{p$nmaforest$pairs}.
}
\description{
\code{nmaforest()} draws what \code{\link[gemtc:mtc.run]{gemtc::mtc.run()}} estimated: one row per
comparison, the posterior median and its credible interval, and, next to
them, the direct evidence behind that pair -- how many studies randomised
the two treatments against each other and how many patients they carried.
Treatment names are printed as names: underscores become spaces, and the
descriptions \code{\link[=nma_gemtc]{nma_gemtc()}} keeps are used when they are there.
}
\details{
Three sets of comparisons, chosen with \code{comparisons}:
\describe{
\item{\code{"panels"}}{every pairwise comparison, in one block per comparator,
each headed "Compared with ..." (the default).}
\item{\code{"reference"}}{one block, every treatment against \code{reference} --
the layout \code{\link[gemtc:forest]{gemtc::forest()}} draws.}
\item{\code{"pairs"}}{one flat block, one row per unordered pair, "A vs B".}
}

Two looks, chosen with \code{style}: \code{"jama"} (serif, open markers, hairline
rules) and \code{"revman"} (sans, filled squares sized by the direct sample
size, events over totals when the outcome is binary).
}
\examples{
\donttest{
if (requireNamespace("gemtc", quietly = TRUE) &&
    requireNamespace("rjags", quietly = TRUE)) {
  data(mash)
  d <- mash$fib_improvement_alldoses
  network <- nma_gemtc(d[, c("study", "treatment", "responders", "sampleSize")])
  model <- gemtc::mtc.model(network, likelihood = "binom", link = "log")
  result <- gemtc::mtc.run(model, n.adapt = 1000, n.iter = 4000)
  nmaforest(result, comparisons = "reference", reference = "Placebo",
            outcome = "Fibrosis improvement")
}
}
}
\seealso{
\code{\link[=nmaplot]{nmaplot()}} for the evidence network, \code{\link[=nma_gemtc]{nma_gemtc()}} for building
the network with the treatment names as they are.
}
