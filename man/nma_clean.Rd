% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean.R
\name{nma_clean}
\alias{nma_clean}
\title{Convert numbers stored as text}
\usage{
nma_clean(data, columns = NULL)
}
\arguments{
\item{data}{A data frame, for example the rows of \code{\link[meta:pairwise]{meta::pairwise()}} read
back from a file.}

\item{columns}{Optional names of the columns to convert. Defaults to all.}
}
\value{
\code{data}, with the numeric-looking text columns converted to numbers.
}
\description{
Files written in locales that use a decimal comma (\code{write.csv2()}, most
spreadsheets) come back from \code{read.csv()} with effect sizes such as
\code{"-0,684"} stored as text, which \code{netmeta::netmeta()} rejects
("Non-numeric value for argument 'TE'"). \code{nma_clean()} turns every text
column that holds only numbers into a numeric column.
}
\details{
A comma is read as the decimal mark; when a value has both a dot and a
comma (\code{"1.234,5"}) the dot is taken as a thousands separator. The Unicode
minus sign and the dashes that word processors and spreadsheets substitute
for \code{-} are accepted, and spaces (including non-breaking ones) are ignored.
Columns with any non-numeric entry (treatment names, notes, a lone \code{"-"})
and columns that are already numeric are returned unchanged.

\code{\link[=nmaplot]{nmaplot()}} applies it for you when given pairwise data.
}
\examples{
f <- system.file("extdata", "recurrence_pairwise.csv", package = "nmaplots")
d <- read.csv(f, sep = ";", fileEncoding = "UTF-8")
class(d$TE)
d <- nma_clean(d)
class(d$TE)
}
