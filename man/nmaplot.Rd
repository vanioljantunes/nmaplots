% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nmaplot.R
\name{nmaplot}
\alias{nmaplot}
\title{Network plot for a netmeta object}
\usage{
nmaplot(
  x,
  layout = c("multi", "circle", "star"),
  order = NULL,
  reference = NULL,
  labels = NULL,
  show_n = TRUE,
  ring = NULL,
  ring_colors = NULL,
  ring_width = 0.45,
  ring_labels = TRUE,
  ring_label_min = 0.05,
  ring_name = NULL,
  ring_title = NULL,
  outcome = NULL,
  summary_line = TRUE,
  font_family = "serif",
  label_wrap = 18,
  label_size = 14,
  label_color = "#1F2A44",
  label_offset = 0.06,
  node_size = c("n", "studies", "equal"),
  node_size_range = c(0.07, 0.2),
  node_fill = "#A94A47",
  reference_fill = "#8A939B",
  node_color = "white",
  node_stroke = 0.8,
  palette = "Dark 3",
  highlight = NULL,
  highlight_color = "#E4572E",
  edge_width = c("studies", "equal"),
  edge_width_range = c(0.5, 3),
  edge_color = "black",
  edge_alpha = 1,
  edge_style = c("single", "multi"),
  max_lines = 6,
  min_studies = 1,
  edge_labels = TRUE,
  edge_label_size = 11,
  edge_label_color = "black",
  edge_label_fill = "white",
  edge_label_offset = 0.05,
  multiarm = FALSE,
  multiarm_fill = "#8A939B",
  multiarm_alpha = 0.08,
  title = "Network of Interventions",
  subtitle = NULL,
  caption = NULL,
  title_size = 22,
  title_color = "#1F2A44",
  title_align = c("center", "left"),
  circle_guide = TRUE,
  circle_color = "#D5D8DC",
  grid = FALSE,
  grid_color = "grey88",
  background = "white",
  legend = TRUE,
  legend_size = 11,
  margin = 0.06,
  file = NULL,
  width = 9,
  height = 8,
  dpi = 300,
  ...
)
}
\arguments{
\item{x}{An object of class \code{netmeta} (from \code{\link[netmeta:netmeta]{netmeta::netmeta()}}).}

\item{layout}{Node arrangement. \code{"multi"} (default): treatments evenly
spaced on a polygon, in the order of \code{order}. \code{"circle"}: treatments on
a circle (drawn as a light guide line), ordered by number of direct
comparisons, most connected treatment at the top and going clockwise.
\code{"star"}: reference treatment in the centre, the others on a polygon
around it. A numeric matrix with two columns and
one row per treatment (row names = treatment names) gives a custom
layout.}

\item{order}{Character vector giving the order of treatments around the
polygon (used by \code{"multi"} and \code{"star"}). Defaults to the order stored in
the \code{netmeta} object. Partial vectors are completed with the remaining
treatments.}

\item{reference}{Treatment placed in the centre for \code{layout = "star"} and
filled with \code{reference_fill}. Defaults to the reference group of the
\code{netmeta} object.}

\item{labels}{Optional character vector of display names for the
treatments, in the order of \code{x$trts} (or a named vector).}

\item{show_n}{Logical. Print the sample size and its share of all
randomised participants (\verb{n = 1,234 (18\% of total)}) under each
treatment name. When the \code{netmeta} object has no sample sizes the number
of studies is printed instead (\code{k = 4}).}

\item{ring}{Optional subgroup composition drawn as a ring around each node.
Either a long data frame with three columns (treatment, group, value) or
a wide matrix / data frame with one row per treatment (row names) and one
column per group. Values are counts or proportions; each treatment is
normalised to 100 percent. Treatments missing from \code{ring} get no ring.}

\item{ring_colors}{Colours for the ring groups: a named vector (names =
groups) or a vector in the order of the groups. Defaults to a muted
blue / red / green set.}

\item{ring_width}{Thickness of the ring as a fraction of the node radius
(never thinner than 0.035 layout units so small nodes stay readable).}

\item{ring_labels}{Logical. Print the percentage of each ring segment
outside the ring.}

\item{ring_label_min}{Segments below this proportion get no percentage
label.}

\item{ring_name}{Short name of what the ring shows (e.g. \code{"Risk of bias"}).
Used in the subtitle and in the legend ("Outer ring = Risk of bias").}

\item{ring_title}{Title of the ring section in the legend. Defaults to
\code{"Outer ring = <ring_name>"}.}

\item{outcome}{Name of the outcome. Becomes the subtitle (with the ring
name appended when a ring is drawn) unless \code{subtitle} is given.}

\item{summary_line}{Logical. Add a line under the subtitle with the number
of studies, treatments and patients in the network.}

\item{font_family}{Font family for all text. \code{"serif"} reproduces the
reference look; use \code{"sans"} for Helvetica-like output.}

\item{label_wrap}{Integer. Wrap treatment names longer than this many
characters. \code{NULL} disables wrapping.}

\item{label_size}{Font size (points) of the treatment names and of the
sample-size line under them.}

\item{label_color}{Colour of the treatment names.}

\item{label_offset}{Gap between the node (or its ring) and the label, in
layout units (the network spans roughly -1 to 1).}

\item{node_size}{What drives the node area: \code{"n"} (sample size),
\code{"studies"} (number of studies including the treatment) or \code{"equal"}.
A numeric vector (length one or one per treatment) gives radii directly.}

\item{node_size_range}{Smallest and largest node radius, in layout units.}

\item{node_fill}{Fill colour of the nodes: a single colour, one colour per
treatment (in the order of \code{x$trts} or named), or \code{"auto"} to use
\code{palette}.}

\item{reference_fill}{Fill colour of the reference treatment (\code{NULL} to
use \code{node_fill}).}

\item{node_color}{Border colour of the nodes.}

\item{node_stroke}{Border width of the nodes (mm).}

\item{palette}{Colours used when \code{node_fill = "auto"}: a colour vector or
the name of a \code{\link[grDevices:hcl.colors]{grDevices::hcl.colors()}} palette.}

\item{highlight}{Optional treatments to emphasise with \code{highlight_color}.}

\item{highlight_color}{Fill colour for highlighted treatments.}

\item{edge_width}{What drives the edge width: \code{"studies"} or \code{"equal"}, or
a single number (mm).}

\item{edge_width_range}{Thinnest and thickest edge in mm.}

\item{edge_color}{Edge colour (single, or one per direct comparison).}

\item{edge_alpha}{Edge opacity.}

\item{edge_style}{\code{"single"} draws one line per comparison with width by
number of studies; \code{"multi"} draws one parallel line per study (up to
\code{max_lines}).}

\item{max_lines}{Maximum parallel lines per comparison for
\code{edge_style = "multi"}.}

\item{min_studies}{Hide direct comparisons with fewer studies than this.}

\item{edge_labels}{Logical. Print the number of studies on each edge.}

\item{edge_label_size}{Font size (points) of the edge labels.}

\item{edge_label_color}{Text and border colour of the edge labels.}

\item{edge_label_fill}{Fill of the circle that carries the study count on
each edge. \code{NA} prints plain text nudged off the line instead.}

\item{edge_label_offset}{Nudge (layout units) for plain-text edge labels.}

\item{multiarm}{Logical. Shade the polygon of each multi-arm study.}

\item{multiarm_fill, multiarm_alpha}{Fill and opacity of those polygons.}

\item{title, subtitle, caption}{Title, subtitle and caption. \code{title}
defaults to "Network of Interventions"; \code{subtitle} defaults to \code{outcome}.
The caption is printed in italics at the bottom right.}

\item{title_size}{Font size of the title. Subtitle, caption and legend
text scale from it.}

\item{title_color}{Colour of the title.}

\item{title_align}{\code{"left"} or \code{"center"}.}

\item{circle_guide}{Logical. For \code{layout = "circle"}, draw the circle the
nodes sit on as a light dashed line.}

\item{circle_color}{Colour of that guide line.}

\item{grid}{Logical. Light background grid.}

\item{grid_color}{Colour of the grid lines.}

\item{background}{Background colour.}

\item{legend}{Logical. Draw the boxed legend under the network (node size,
edge width, ring groups). On by default.}

\item{legend_size}{Font size (points) of the legend text.}

\item{margin}{Extra space around the network as a fraction of its radius.}

\item{file}{Optional path(s) to save the plot; format from the extension
(\code{.png}, \code{.pdf}, \code{.tiff}/\code{.tif}).}

\item{width, height}{Size of the saved figure in inches.}

\item{dpi}{Resolution of raster output.}

\item{...}{\code{\link[netmeta:netgraph]{netmeta::netgraph()}} arguments accepted for compatibility:
\code{col.points}, \code{col}, \code{number.of.studies}, \code{cex}, \code{cex.points}, \code{seq},
\code{start.layout}, \code{thickness}, \code{labels}; \code{plastic}, \code{points}, \code{lwd},
\code{points.min}, \code{points.max} are ignored. Unknown arguments are ignored
with a message.}
}
\value{
A \code{ggplot} object with an extra element \code{nmaplot} holding the node,
edge and ring data frames used for drawing. Returned invisibly when
\code{file} is given.
}
\description{
Draws the evidence network of a network meta-analysis fitted with
\code{\link[netmeta:netmeta]{netmeta::netmeta()}}. The function takes the same object as
\code{\link[netmeta:netgraph]{netmeta::netgraph()}} and renders it with 'ggplot2': node area by sample
size, edge width by number of studies, study counts on the edges, treatment
name and sample size at every node, an optional outer ring showing a
subgroup composition per treatment (for example study design), several
layouts, a bottom legend panel, and direct export to PNG, PDF or TIFF.
}
\examples{
if (requireNamespace("netmeta", quietly = TRUE)) {
  library(netmeta)
  data(mash)
  d <- mash$fib_improvement_alldoses
  p <- pairwise(treat = treatment, event = responders, n = sampleSize,
                studlab = study, data = d, sm = "RR")
  net <- netmeta(p, reference.group = "Placebo")
  nmaplot(net, outcome = "Fibrosis improvement without worsening of MASH")

  # outer ring from a column of the data (here study design)
  nmaplot(net, ring = table(d$treatment, d$design), ring_name = "Study design",
          outcome = "Fibrosis improvement without worsening of MASH")
}

}
